// // Assignment code here


// // Get references to the #generate element
// var generateBtn = document.querySelector("#generate");

// // Write password to the #password input
// function writePassword() {
//   var password = generatePassword();
//   var passwordText = document.querySelector("#password");

//   passwordText.value = password;

// }

// // Add event listener to generate button
// generateBtn.addEventListener("click", writePassword);
var chars = "abcdefghijklmnopqrstuvwxyz";
var specialChars = "!@#$%^&*()";
var numChars = "0123456789";
var upperChars = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";
function password(){
  if(window.confirm("want special charachters")){
// adds the specialchar into the equation and continues the process
window.confirm("want upper case chars")
  }
else {window.confirm("want upper case chars")}
}

function start(){
  if( window.confirm("need a new password")){
    password();
  }
}
